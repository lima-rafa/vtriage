name: oss-validate

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  zipcpu-real:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout vtriage
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install vtriage (editable)
        run: |
          python -m pip install -U pip
          python -m pip install -e .

      - name: System deps (Verilator + libs)
        run: |
          sudo apt-get update
          sudo apt-get install -y verilator g++ make libelf-dev libncurses-dev git

      - name: Clone ZipCPU
        run: |
          mkdir -p _oss
          git clone --depth 1 https://github.com/ZipCPU/zipcpu.git _oss/zipcpu

      # Aqui a gente faz run seeds=1 (porque é um “teste real”, não seed-based)
      # e permite falha sem quebrar o job, pra sempre gerar report/artifacts.
      - name: vtriage run (ZipCPU real build/test) -> analyze -> report (allow fail)
        shell: bash
        run: |
          vtriage run \
            --seeds 1 \
            --cmd "bash -lc 'make rtl && make -C sim/verilator div_tb mpy_tb && ./sim/verilator/div_tb && ./sim/verilator/mpy_tb'" \
            --workdir _oss/zipcpu \
            --artifact-root artifacts \
            --out report \
            --no-analyze || true

          # analisa o run mais recente gerado
          vtriage analyze --latest --out ./report --reuse-index

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: zipcpu-report
          path: report/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: zipcpu-artifacts
          path: artifacts/

      - name: Debug (show last logs on failure)
        if: failure()
        shell: bash
        run: |
          echo "Latest artifacts dir:"
          ls -la artifacts || true
          latest=$(ls -1 artifacts | grep '^run_' | sort -r | head -n 1 || true)
          echo "latest=$latest"
          if [ -n "$latest" ]; then
            find "artifacts/$latest/tests" -name log.txt -maxdepth 2 -print -exec tail -n 120 {} \; || true
          fi

  litex-real:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout vtriage
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install vtriage (editable)
        run: |
          python -m pip install -U pip
          python -m pip install migen
          python -m pip install -e .

      - name: Clone LiteX
        run: |
          mkdir -p _oss
          git clone --depth 1 https://github.com/enjoy-digital/litex.git _oss/litex

      - name: Install LiteX (editable) + deps
        shell: bash
        run: |
          python -m pip install -U pip wheel setuptools
          python -m pip install -e _oss/litex

      # Smoke real: importa litex e roda um comando "leve"
      - name: vtriage run (LiteX smoke) -> analyze -> report (allow fail)
        shell: bash
        run: |
          vtriage run \
            --seeds 1 \
            --cmd "bash -lc 'python -c \"import litex; print(\\\"litex-ok\\\")\"'" \
            --workdir _oss/litex \
            --artifact-root artifacts \
            --out report \
            --no-analyze || true

          vtriage analyze --latest --out ./report --reuse-index

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: litex-report
          path: report/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: litex-artifacts
          path: artifacts/

  vexriscv-real:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout vtriage
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install vtriage (editable)
        run: |
          python -m pip install -U pip
          python -m pip install -e .

      - name: System deps (Java)
        run: |
          sudo apt-get update
          sudo apt-get install -y default-jdk git

      - name: Clone VexRiscv
        run: |
          mkdir -p _oss
          git clone --depth 1 https://github.com/SpinalHDL/VexRiscv.git _oss/vexriscv

      # sbt: usamos o wrapper do próprio repo (se existir) ou instalamos sbt
      - name: Install sbt (if needed)
        shell: bash
        run: |
          if [ -f "_oss/vexriscv/sbt" ]; then
            echo "sbt script exists in repo."
          else
            echo "Installing sbt..."
            sudo apt-get install -y sbt || true
          fi

      - name: vtriage run (VexRiscv sbt smoke) -> analyze -> report (allow fail)
        shell: bash
        run: |
          # Rodar um comando "leve" e real: compile/test do projeto
          # (Se esse alvo mudar, a gente ajusta conforme o log do CI)
          vtriage run \
            --seeds 1 \
            --cmd "bash -lc './sbt -no-colors compile'" \
            --workdir _oss/vexriscv \
            --artifact-root artifacts \
            --out report \
            --no-analyze || true

          vtriage analyze --latest --out ./report --reuse-index

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: vexriscv-report
          path: report/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: vexriscv-artifacts
          path: artifacts/
